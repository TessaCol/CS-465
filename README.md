# CS-465

#Compare and contrast the types of frontend development you used in your full stack project, including Express HTML, JavaScript, and the single-page application (SPA).

The Express HTML side was more simple and server rendered, where pages reload each time and the backend sends the data. The SPA with Angular was more dynamic because it updates the page without reloading and uses components. JavaScript is used in both, but Angular organizes it better and makes it easier to manage larger features.

#Why did the backend use a NoSQL MongoDB database?
MongoDB was used because it stores data in a flexible format that works well with JSON. It made it easier to handle changing data like trips and didn’t require strict table structures like SQL.

#How is JSON different from Javascript and how does JSON tie together the frontend and backend development pieces?
JSON is just a data format, while JavaScript is a programming language. JSON is used to send data between the frontend and backend, so both sides can understand and use the same data structure.

#Provide instances in the full stack process when you refactored code to improve functionality and efficiencies, and name the benefits that come from reusable user interface (UI) components.
I refactored code when I separated the API into its own layer and when I turned trip logic into reusable components in Angular. This made the code cleaner and easier to update. Reusable UI components help save time, reduce repetition, and make the app easier to maintain.

#Methods for request and retrieval necessitate various types of API testing of endpoints, in addition to the difficulties of testing with added layers of security. Explain your understanding of methods, endpoints, and security in a full stack application.
Methods like get, post, put, and delete are used to interact with endpoints that handle specific data requests. Testing these makes sure the correct data is returned or updated. Security adds another layer because endpoints require authentication, so tokens also need to be tested to make sure only authorized users can access certain features.

#How has this course helped you in reaching your professional goals? 
This course helped me understand how full stack applications are built and how different parts of a system work together. It gave me hands on experience that I can use in real world projects and helps me feel more prepared for a job in development.

#What skills have you learned, developed, or mastered in this course to help you become a more marketable candidate in your career field?
I learned how to build APIs, work with databases, create dynamic frontends, and add security using authentication. I also improved my problem solving and debugging skills, which makes me more confident and more valuable as a developer.
